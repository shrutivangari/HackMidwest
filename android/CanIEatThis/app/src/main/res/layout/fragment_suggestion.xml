<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" tools:context="hmw.com.canieatthis.SuggestionFragment"
    android:orientation="vertical">

    <EditText
        android:id="@+id/sugg_text"
        android:imeOptions="actionSearch"
        android:drawableRight="@android:drawable/ic_menu_search"
        android:inputType="text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="oatmeal" />

    <TextView
        android:id="@+id/sugg_result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:textSize="40dp" />

    <TextView
        android:id="@+id/sugg_reason"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/sugg_separator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/sugg_othersugg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:textStyle="bold"/>

    <ScrollView
        android:id="@+id/sugg_scroll"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:fillViewport="true" >

        <ListView
            android:id="@+id/sugg_otherfood"
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            >
        </ListView>

    </ScrollView>


</LinearLayout>

